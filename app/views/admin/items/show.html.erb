<h4>商品詳細</h4>
   <p><%= image_tag @item.image, size: "300x250" %></p>
   
<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>商品名</td>
      <td><%= @item.name %></td>
    </tr>
    <tr>
      <td>商品説明</td>
      <td><%= @item.introduction %></td>
    </tr>
    <tr>
      <td>ジャンル</td>
      <td><%= @item.genre.name %></td>
    </tr>
    <tr>
      <td>税込価格<br>(税抜価格)</td>
      <td><%= number_with_delimiter(@item.with_tax_price) %>
          (<%= number_with_delimiter(@item.price) %>)
      </td>
    </tr>
    <tr>
      <td>販売ステータス</td>
      <td></td>
    </tr>
  </tbody>
</table>

    
  <%= link_to "編集する", edit_admin_item_path(@item), class: 'btn btn-success' %>
